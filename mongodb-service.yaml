#apiVersion: v1
#kind: Service
#metadata:
#  name: mongodb
#spec:
#  selector:
#    app: mongodb
# ports:
#    - port: 27017
#      targetPort: 27017


apiVersion: v1
kind: Service
metadata:
  name: mongodb
spec:
  type: ClusterIP
  selector:
    app: mongodb
  ports:
    - name: mongo
      protocol: TCP
      port: 27017
      targetPort: 27017